<Window x:Class="GeminiModGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeminiModGenerator"
        mc:Ignorable="d"
        Title="Minecraft Forge Mod Generator - V5" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#2D2D30">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mod Name (no spaces):" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
        <TextBox x:Name="ModNameTextBox" Grid.Row="0" Grid.Column="1" Margin="5" Text="MyForgeMod" Background="#3F3F46" Foreground="White" BorderBrush="#555"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Path to Forge Template:" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
        <TextBox x:Name="TemplatePathTextBox" Grid.Row="1" Grid.Column="1" Margin="5" Text="C:\Path\To\Your\forge-mdk" Background="#3F3F46" Foreground="White" BorderBrush="#555"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Your Gemini API Key:" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
        <TextBox x:Name="ApiKeyTextBox" Grid.Row="2" Grid.Column="1" Margin="5" Text="PASTE_YOUR_API_KEY_HERE" Background="#3F3F46" Foreground="White" BorderBrush="#555"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Mod Description:" Foreground="White" FontSize="16" Margin="0,10"/>
        <TextBox x:Name="PromptTextBox" Grid.Row="3" Grid.Column="1" TextWrapping="Wrap" AcceptsReturn="True" Margin="5"
                 Background="#3F3F46" Foreground="White" FontSize="14" BorderBrush="#555"
                 Text="A simple mod that adds a new ore called 'Sapphire' which can be crafted into a Sapphire Sword."/>

        <Button x:Name="GenerateButton" Grid.Row="4" Grid.ColumnSpan="2" Content="Generate and Build Mod"
                FontSize="16" FontWeight="Bold" Margin="0,15,0,15" Padding="10"
                Background="#007ACC" Foreground="White" BorderBrush="#005A9E"
                Click="GenerateButton_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>

        <ScrollViewer Grid.Row="5" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="LogTextBox" TextWrapping="Wrap" IsReadOnly="True"
                       Background="#1E1E1E" Foreground="#D4D4D4"
                       FontFamily="Consolas" FontSize="12"
                       Text="Logs and generated code will appear here..."/>
        </ScrollViewer>
    </Grid>
</Window>